; To do this, you'd need a program that can perform algebraic manipulations on
; expressions to minimise the error. Additionally, there would need to be some
; way to compute the maximum / minimum value of a function over some domain.
; Perhaps this could be done with calculus by restricting to differentiable
; functions (but then we need symbolic differentiation!), but it's not clear
; how this would be performed for a general function.
