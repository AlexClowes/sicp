; Generic operations with explicit dispatch is simple to implement, but
; difficult to extend.

; Data-directed style makes it easy to add new types, but difficult to add new
; operations.

; Message-passing style makes it easy to add new operations, but difficult to
; add new types.
